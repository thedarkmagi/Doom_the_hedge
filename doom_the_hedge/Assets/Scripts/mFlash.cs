using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mFlash : MonoBehaviour
{
    public GameObject mflash;
    // Start is called before the first frame update
    void Awake()
    {
        //var go =Instantiate(mflash, new Vector3(transform.parent.position.x+0.2f, transform.parent.position.y+0.1f, transform.parent.position.z+3.25f),Quaternion.identity);
        //go.transform.parent = transform.parent;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

   public void PlayAnim()
    {
        
    }
}
